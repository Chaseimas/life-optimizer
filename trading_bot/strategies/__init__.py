"""Strategies: venue-agnostic signal generators.

A strategy sees normalized bars and emits directional signals. It never knows
which venue it runs on and never sizes positions — that is the risk engine's
job.
"""
